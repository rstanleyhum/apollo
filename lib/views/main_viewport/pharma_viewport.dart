import 'package:flutter/material.dart';

import '../main_pages/home_page.dart';

class PharmaViewport extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Container(
        child: HomePage(title: "PharmaViewport"),
      );
}
