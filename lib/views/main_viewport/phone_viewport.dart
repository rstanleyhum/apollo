import 'package:flutter/material.dart';

import '../main_pages/home_page.dart';

class PhoneViewport extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Container(
        child: HomePage(title: "PhoneViewport"),
      );
}
